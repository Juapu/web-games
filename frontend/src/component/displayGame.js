import React from 'react';

const DisplayGame = () => {
    // This probably needs a better name.
    // Not sure what this is supposed to do, but creating the file in case.
}

export default DisplayGame;